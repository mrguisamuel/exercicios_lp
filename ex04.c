#include <stdio.h>

#define MAX 10

int main() {
  char letras[MAX];
  int consoantes;
  int i;
  for(i = 0; i < MAX; i++) {
    printf("Digite a letra: ");
    scanf("%c", &letras[i]);
  }
  printf("Consoantes: ");
  for(i = 0; i < MAX; )
}